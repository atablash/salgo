#define USE_FAST_IO true
#include "../../common.hpp" // competitive programming macros, etc.

#include <salgo/all>
#include <bits/stdc++.h>

using namespace salgo;
using namespace std;



int main() {

	is_prime();

	/*
	RI; // skip n

	char c = 0;
	while(!isgraph(c)) {
		cin >> c;
	}

	Sequence_Hash hash;
	Sequence_Hash hash_rev;

	while(isgraph(c)) {
		hash.push_back(c);
		hash_rev.push_front(c);

		cin >> c;
	}

	cout << (hash == has_rev ? "TAK" : "NIE") << endl;
	*/

	return 0;
}

